defaults: &defaults
  adapter: sqlite

development:
  database: db/asset_pipeline_development.db
  <<: *defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgres
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: db/asset_pipeline_test.db
  <<: *defaults
production:
  database: db/asset_pipeline_production.db
  <<: *defaults
